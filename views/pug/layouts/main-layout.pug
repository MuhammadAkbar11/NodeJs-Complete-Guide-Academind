<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{ pageTitle }
    body
        header
            a(href="/") Enter User  
            span | 
            a(href="/users") Users
        br
        br
        block content